# Udacity React Developer Nano Degree
## Mobile Flash Cards

### Installation (Tested for Android)
use: `yarn install` or `npm install`

### Launch App (Tested for Android)
use: `yarn andriod` or `yarn start` or `npm start`